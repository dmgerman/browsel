# build/ is materialised by `make build` and is committed in this
# repository so users do not have to rebuild before using the
# extension.  Source files live alongside the Makefile; edit those,
# then re-run `make build` to refresh build/.
